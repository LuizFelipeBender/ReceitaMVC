@using Microsoft.AspNetCore.Identity
@using SiteMVC.Data
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@if (SignInManager.IsSignedIn(User) && User.HasClaim(x => x.Type == "IsAdmin" && x.Value == "true"))
{
    <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Admin
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <a class="dropdown-item" asp-controller="Admin" asp-action="ListaUsuarios">Users</a>
            <a class="dropdown-item" asp-controller="Admin" asp-action="ListaIngredientes">Ingredientes</a>
        </div>
    </li>
}
